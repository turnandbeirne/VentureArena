{
  "name": "venturemaker-arena",
  "private": true,
  "version": "0.1.0",
  "description": "VentureMaker Arena — generic multiplayer platform (accounts, rooms, matchmaking) for VentureMaker games, starting with VentureFlow. See README.md.",
  "type": "module",
  "scripts": {
    "build:engine": "node scripts/bundle-game-engine.mjs",
    "sync:engine": "bash scripts/sync-game-engine.sh",
    "sync:app-vendor": "bash scripts/sync-app-vendor.sh",
    "test:engine": "node scripts/test-engine-bundle.mjs",
    "test:ai-chain": "node scripts/test-ai-chain-resolution.mjs",
    "test": "npm run test:engine && npm run test:ai-chain"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
